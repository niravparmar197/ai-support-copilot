# experiments

Notes from evaluation experiments — RAG quality, prompt variants, agent
behavior — starting Day 33 onward once there's something to evaluate.

This is where `/eval`, `rag-evaluator`, and `prompt-reviewer` output real
findings once they stop being stubs. Empty until then; that's expected, not
a gap to fill early.
