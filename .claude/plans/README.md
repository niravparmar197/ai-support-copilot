# plans

The plan output from `/feature`, before implementation starts.

`/feature <name>` proposes a short implementation plan and waits for
confirmation before writing any code (see `.claude/commands/feature.md`).
Confirmed plans land here so there's a record of what was actually agreed
to, separate from the `specs/` written ahead of a phase and separate from
the eventual code.
