---
description: Run the Ragas/promptfoo evaluation suite (stub until Day 33+)
---

**Stub.** The real evaluation suite (Ragas/promptfoo against the AI
service's RAG pipeline) doesn't exist yet — it's planned for Day 33+ per the
project plan, once `ai-service/` and retrieval exist.

Until then, this command should just tell the user that: report there's no
evaluation suite to run yet, and point to `.claude/experiments/` as where
eval notes will start landing once this is real. Don't invent an eval run.
