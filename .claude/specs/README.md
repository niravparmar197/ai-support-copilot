# specs

Written specs, produced before implementing a phase — not after.

Per `CLAUDE.md`'s architectural-decisions rule, anything that would be
annoying or expensive to reverse gets thought through and written down here
before code exists, not reverse-engineered from the implementation
afterward. A spec here is expected to be short: what's being built, the
interface/contract, and what's explicitly out of scope.
